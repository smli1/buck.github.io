import { calculateManualScenario, rollSecondWind, rollRanged, rollMeleeCombo, manualValues, manualMissFlags, getManualNumber, setManualValue } from './manual-calculator.js';
import { 
    initializeManualButtons, 
    openManualPopup, 
    closeManualPopup, 
    clearManualPopup, 
    recordManualMiss, 
    selectManualField, 
    setManualDiceValue,
    setManualScenario 
} from './manual-ui.js';

export {
    calculateManualScenario,
    rollSecondWind,
    rollRanged,
    rollMeleeCombo,
    manualValues,
    manualMissFlags,
    getManualNumber,
    setManualValue,
    initializeManualButtons,
    openManualPopup,
    closeManualPopup,
    clearManualPopup,
    recordManualMiss,
    selectManualField,
    setManualDiceValue,
    setManualScenario
};
